node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.nitro
.tanstack
.output
.vinxi
todos.json

# Ignore old index_out directory (deprecated)
public/index_out/

# Keep versioned MTG embeddings data (committed to repo for deployment)
# These files are large but necessary for the app to function
# Update version when copying new embeddings from packages/mtg-image-db
!public/data/
!public/data/mtg-embeddings/
!public/data/mtg-embeddings/**

test-results/
.playwright-storage/*.demo.ts

certificates/

.playwright-storage/
playwright-report/
results.xml

public/latest-dev/

# Copied from repo-root static/card-index/ at build time
public/card-index/

.vercel
